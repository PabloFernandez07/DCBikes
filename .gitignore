# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Supabase CLI temp/cache (regenerados al `supabase link`)
supabase/.temp/

# Documentos de trabajo del cliente (Excel/Word importables, no van al repo)
Docs/*.xlsx
Docs/*.xls
Docs/*.docx
Docs/*.doc

# Mantenemos los markdown/html de Docs (auditoría legal, testing report, etc.)
!Docs/**/*.md
!Docs/**/*.html

# S-15: runbook real con mapeo UUID→persona (PII). Solo el .template se commitea.
Docs/runbooks/admin-users-seed.md

# Secretos generados localmente — NO COMMITEAR
Docs/runbooks/secretos-generados-RECUPERAR.txt

# Copias de seguridad de datos antes de operaciones destructivas.
# Contienen datos de clientes: NUNCA al repositorio.
.backups/

# Generado en cada build por scripts/generate-sitemap.mjs (prebuild). Estuvo
# versionado y un build local sin credenciales lo reescribía con 15 URLs en vez
# de 129, listo para colarse en un commit. Vercel lo regenera siempre.
public/sitemap.xml
